name: My First Workflow

on: [push]

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, World!"
      - run: ${{ secrets.HELLO }}
  build: 
    runs-on: ubuntu-latest
    steps:
      - run: echo "Building..."
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Testing..."
